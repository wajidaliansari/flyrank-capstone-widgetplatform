run: "uvicorn app.main:app --reload"
seed: "Use the Swagger UI at /docs to POST a new widget"
test: "pytest"
base_url: "http://127.0.0.1:8000"
endpoints:
  - "POST /widgets/"
  - "GET /widgets/{id}/config"
  - "POST /submissions"
  - "GET /widgets/{id}/dashboard"