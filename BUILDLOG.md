# AI Usage Log

* **Where AI helped:** I used an AI assistant to help debug CORS errors, troubleshoot SQLite schema mismatches, and format Pydantic models.
* **Where AI was wrong:** The AI sometimes suggested terminal commands that locked my database file on Windows.
* **What I changed:** I manually managed my terminal sessions, deleted the locked `widgets.db` file, and hardcoded the ID sync between my HTML and backend.